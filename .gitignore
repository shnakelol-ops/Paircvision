# Dependencies
node_modules/

# Build output
dist/

# Editor / OS
.DS_Store
*.local

# TypeScript build info
*.tsbuildinfo

# Scratch PDFs written to the repo root by src/stats/snapshotExport.test.ts
# (cleaned up in its afterEach, but can race under parallel test workers)
/Adare_v_Mungret_*.pdf
